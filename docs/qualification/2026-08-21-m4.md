# MVP qualification run — 2026-08-21

## Environment

- Release Rust build, commit base `e0d26a2` plus #21 tuning in this branch
- macOS 26.6.1 (25G76)
- MacBook Air Mac16,12, Apple M4, 10 cores, 24 GB RAM
- Corpus: the real `/Users/liyixin` home tree on local APFS (not generated data)
- Published generation: 1,170,651 entries; 138 skipped locations

This machine is not the required base M1/8 GB acceptance machine. Results below
are useful regression evidence but cannot satisfy that hardware-specific gate.

## Results

| Gate | Result | Status |
|---|---:|---|
| Shortcut-to-interactive | 12.0–13.1 ms observed | Pass on this host |
| Existing-index application-ready | 84 ms observed; committed rows visible immediately | Pass on this host |
| Initial scan | exceeded 1,042,708 observed entries after about 30 s; 1,170,651 published in under 90 s | Pass on this host |
| Quick Search during scan | interactive window remained available with truthful Rebuilding progress | Pass |
| Correctness/recovery suite | 55 tests passed | Pass |
| 10,000 pending hint drain | about 0.01 s | Pass |
| SQLite + projection | 632,908 KiB + 426,892 KiB before generation pruning | Fail |
| RSS | 1,775,968 KiB during Catching Up; 2,587,148,288-byte measured peak | Fail |
| Current CPU / idle | corpus continuously generated Home-directory events and remained Catching Up | Not qualified |
| Every sort p95/p99 | no release-harness measurements yet | Not qualified |
| Balanced small-change visibility | covered by live UI checks on smaller real fixtures; not repeated on this corpus | Not qualified here |

## Findings and tuning applied

The first run found that every reconciliation retained all historical full
generations. The #21 branch now deletes generations no longer referenced by
`published_roots` in the publication transaction and enables full auto-vacuum for
new File Indexes. Existing-index startup now opens a generation-validated Search
Projection directly instead of loading every SQLite row and rebuilding the
projection before showing committed results.

The remaining disk and Catching Up RSS failures require a more compact projection
and streaming reconciliation; those are product changes, not evidence that can be
papered over in a qualification report.

## Repeat commands

```sh
scripts/build-app.sh release
mkdir -p /private/tmp/everyfile-qualification
EVERYFILE_INDEX_ROOT=/Users/liyixin \
EVERYFILE_DATA_DIR=/private/tmp/everyfile-qualification \
target/release/Everyfile.app/Contents/MacOS/Everyfile

sqlite3 /private/tmp/everyfile-qualification/index.sqlite3 \
  'select count(*) from entries where generation=(select max(generation) from scan_generations where committed=1);'
du -k /private/tmp/everyfile-qualification/index.sqlite3 \
      /private/tmp/everyfile-qualification/search.projection
pgrep -n -f 'Everyfile.app/Contents/MacOS/Everyfile' | \
  xargs ps -o pid=,rss=,%cpu=,etime= -p
cargo test
cargo clippy --all-targets -- -D warnings
```

For final acceptance, repeat on a base M1/8 GB Mac with a stable representative
one-million-entry APFS corpus, collect query latency for all six sorts, sample idle
CPU for 60 seconds, and attach the raw output to #21.
